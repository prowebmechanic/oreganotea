import type React from 'react';

const LogoSection: React.FC = () => {
  return (
    <div className="bg-black p-2.5 border border-white h-full flex items-center justify-center">
      <h1 className="text-3xl font-bold text-accent">OrgaNote</h1>
    </div>
  );
};

export default LogoSection;
